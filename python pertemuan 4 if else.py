import math
radius = float(input('Enter the radius: '))
if radius <= 0:
    print('ERROR: Input number must be positive! ')
else:
    area = mat.pi * radius ** 2
    print(area)
               
